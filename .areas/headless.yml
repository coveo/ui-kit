description: Headless library package
file_patterns:
  - packages/headless/**
  - packages/headless-react/**
  - packages/bueno/**
  - packages/auth/**

  - samples/headless/**
reviewers:
  dxui: ~

review_bypass:
  # Renovate @ Coveo (installation id 30846434). Dependency PRs are gated on
  # green CI instead of a DXUI review.
  integration/30846434: pull_request
