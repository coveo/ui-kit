{
  "name": "@samples/headless-ssr-commerce-express",
  "description": "Headless Commerce SSR Generic Sample using the new simplified ssr-next API",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm run build:client -- --watch=forever & npx tsx watch ./src/server.ts",
    "build": "pnpm run build:server && pnpm run build:client",
    "build:server": "tsc",
    "build:client": "esbuild ./src/client.ts --bundle --outfile=dist/client.js --format=esm --target=es2022",
    "start": "pnpm run build && npx tsx ./src/server.ts",
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@coveo/headless": "3.30.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "@types/express": "^4.17.17",
    "@types/node": "^20.0.0",
    "esbuild": "^0.25.9",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0"
  },
  "type": "module"
}
