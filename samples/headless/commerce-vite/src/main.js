import './style.css';
// Importing the app shell registers every custom element in the tree
// (<commerce-app> pulls in the pages, which pull in their components).
import './components/commerce-app.js';
