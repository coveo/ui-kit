---
'@coveo/headless': minor
---

Added `proxyBaseUrl` support for the Answer API (GenAI streaming) endpoint. You can now configure a `knowledge.proxyBaseUrl` in the search engine or insight engine configuration to route Answer API requests through a proxy server, similar to how `search.proxyBaseUrl` works for search requests.
