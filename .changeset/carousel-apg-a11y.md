---
'@coveo/atomic': patch
---

Add WAI-ARIA Carousel pattern attributes (`role="region"`, `aria-roledescription="carousel"`, `aria-live="polite"`) to the image carousel component for screen reader accessibility.
