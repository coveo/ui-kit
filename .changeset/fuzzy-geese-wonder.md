---
'@coveo/atomic': patch
---

Implement the WAI-ARIA Tabs APG pattern for `atomic-insight-tab` and `atomic-ipx-tab`, including host-level tab semantics and keyboard navigation in the associated tablists.
