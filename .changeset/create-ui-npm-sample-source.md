---
'@coveo/create-ui': patch
---

Scaffold templates from their published npm package — resolving the package's `latest` release from the registry and extracting it — instead of downloading the `coveo/ui-kit` GitHub monorepo tarball.
