---
'@coveo/atomic': patch
---

Improve keyboard and assistive technology access in the generated answer. The conversation scroll container is now keyboard focusable, the show more/less button references the content it controls, the thread item disclosure button points at the element that is actually hidden, and decorative timeline graphics are no longer exposed to screen readers.
