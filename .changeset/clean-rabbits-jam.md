---
'@coveo/atomic': patch
---

Fix search tab accessibility by aligning `atomic-tab-manager` and `atomic-tab-button` with the WAI-ARIA Tabs pattern, including proper `tablist`/`tab` semantics and keyboard navigation in tab stories.
