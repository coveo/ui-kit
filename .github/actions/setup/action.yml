#region Metadata
name: 'Setup'
description: 'Setup node and install dependencies'
#endregion
runs:
  using: composite
  steps:
    - name: Resolve Thermidor schema source
      run: node scripts/setup-thermidor-schema.mjs
      shell: bash
    #region Mise (Node + pnpm)
    - name: Install tools via mise
      uses: step-security/mise-action@6e96d2ffbc65c037f23c78818f2a339d6cf830f7 # v4
      with:
        install: true
        cache: true
    - name: Get pnpm store directory
      id: pnpm-store
      run: echo "path=$(pnpm store path)" >> "$GITHUB_OUTPUT"
      shell: bash
    - name: Cache pnpm store
      uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830 # v4
      with:
        path: ${{ steps.pnpm-store.outputs.path }}
        key: ${{ runner.os }}-pnpm-store-${{ hashFiles('pnpm-lock.yaml', 'mise.toml') }}
        restore-keys: |
          ${{ runner.os }}-pnpm-store-
    - run: pnpm install --frozen-lockfile --prefer-offline
      shell: bash
    #endregion
