name: 'Playwright Atomic'
description: 'Run Playwright tests for Atomic'
inputs:
  shardIndex:
    description: 'The shard index to run'
    required: true
  shardTotal:
    description: 'The total number of shards'
    required: true
  testsToRun:
    description: 'The tests to run'
    required: true
  uploadArtifacts:
    description: 'Whether to upload artifacts'
    required: false
    default: 'true'
runs:
  using: composite
  steps:
    - name: Run Playwright tests
      id: playwright
      run: pnpm exec playwright test ${{ inputs.testsToRun }} --shard=${{ inputs.shardIndex }}/${{ inputs.shardTotal }}
      working-directory: packages/atomic
      shell: bash
      continue-on-error: true
    - uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4 # v5
      if: ${{ inputs.uploadArtifacts == 'true' }}
      with:
        name: atomic-blob-report-${{ matrix.shardIndex }}
        path: packages/atomic/blob-report
        retention-days: 5
    # https://docs.github.com/en/actions/writing-workflows/choosing-what-your-workflow-does/accessing-contextual-information-about-workflow-runs#steps-context
    - run: |
        if [[ ${{ steps.playwright.outcome}} == 'failure' ]];
          then exit 1;
          else exit 0;
        fi
      shell: bash
