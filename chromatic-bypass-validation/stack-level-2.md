# Chromatic bypass validation: stack level 2

This temporary non-UI change verifies zero-billed bypass behavior when the pull request is based on another bypass candidate.

Do not merge this validation file.
