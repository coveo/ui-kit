{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.thermidor.coveo.com/definitions/comparison-attribute.schema.json",
  "title": "ComparisonAttribute",
  "description": "Defines an attribute (column) used in a ComparisonTable. The key is the programmatic identifier that maps to values in ComparisonProduct.values, and label is the human-readable column heading.",
  "type": "object",
  "required": ["key", "label"],
  "properties": {
    "key": {
      "type": "string",
      "description": "The programmatic identifier for this attribute, used as a key in ComparisonProduct.values."
    },
    "label": {
      "type": "string",
      "description": "The human-readable column heading displayed in the comparison table."
    }
  },
  "additionalProperties": false
}
