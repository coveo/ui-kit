{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.thermidor.coveo.com/definitions/comparison-attribute.schema.json",
  "title": "ComparisonAttribute",
  "description": "Defines an attribute (row) used in a ComparisonTable. The key is the programmatic identifier that maps to values in each compared product's values record, and label is the human-readable row heading.",
  "type": "object",
  "required": ["key", "label"],
  "properties": {
    "key": {
      "type": "string",
      "description": "The programmatic identifier for this attribute, used as a key in each compared product's values record."
    },
    "label": {
      "type": "string",
      "description": "The human-readable column heading displayed in the comparison table."
    }
  },
  "additionalProperties": false
}
