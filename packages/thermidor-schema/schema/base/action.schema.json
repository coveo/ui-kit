{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.thermidor.coveo.com/base/action.schema.json",
  "title": "Action",
  "description": "An Action is the only mechanism through which the frontend can trigger a state change on a Controller. Actions are emitted by the frontend and processed by the backend. The action name is the key in the Controller's actions map and payload carries the runtime parameters.",
  "type": "object",
  "required": ["payload"],
  "properties": {
    "payload": true
  },
  "additionalProperties": false
}
