{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.thermidor.coveo.com/controllers/bundle-display.schema.json",
  "title": "BundleDisplayControllerContract",
  "description": "Owns the tiered product bundle layout. Read-only: the frontend cannot modify tiers directly.",
  "required": ["controllerSchema"],
  "properties": {
    "controllerSchema": {
      "type": "string",
      "const": "https://schema.thermidor.coveo.com/controllers/bundle-display.schema.json"
    },
    "state": {"$ref": "#/$defs/BundleDisplayState"},
    "actions": {
      "type": "object",
      "properties": {},
      "additionalProperties": false
    }
  },
  "additionalProperties": false,
  "allOf": [{"$ref": "https://schema.thermidor.coveo.com/base/controller.schema.json"}],
  "$defs": {
    "BundleDisplayState": {
      "title": "BundleDisplayState",
      "description": "The tiered product bundle layout. Owned exclusively by the backend; the frontend must not mutate this directly.",
      "$comment": "This schema validates the runtime state value the backend pushes to the frontend.",
      "type": "object",
      "required": ["tiers"],
      "properties": {
        "tiers": {
          "type": "array",
          "description": "Ordered list of bundle tiers to render.",
          "items": {
            "$ref": "https://schema.thermidor.coveo.com/definitions/bundle-tier.schema.json"
          }
        }
      },
      "additionalProperties": false
    }
  }
}
