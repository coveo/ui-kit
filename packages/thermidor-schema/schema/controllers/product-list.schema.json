{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.thermidor.coveo.com/controllers/product-list.schema.json",
  "title": "ProductListControllerContract",
  "description": "Owns the ordered list of products to display. Action-less: the frontend cannot directly mutate the product list. Products are updated exclusively via backend state pushes.",
  "required": ["controllerSchema"],
  "properties": {
    "controllerSchema": {
      "type": "string",
      "const": "https://schema.thermidor.coveo.com/controllers/product-list.schema.json"
    },
    "state": {"$ref": "#/$defs/ProductListState"},
    "actions": {
      "type": "object",
      "properties": {},
      "additionalProperties": false
    }
  },
  "additionalProperties": false,
  "allOf": [{"$ref": "https://schema.thermidor.coveo.com/base/controller.schema.json"}],
  "$defs": {
    "ProductListState": {
      "title": "ProductListState",
      "description": "The ordered list of products to render. Owned exclusively by the backend; the frontend must not mutate this directly.",
      "$comment": "This schema validates the runtime state value the backend pushes to the frontend.",
      "type": "object",
      "required": ["products"],
      "properties": {
        "products": {
          "type": "array",
          "description": "Ordered list of products to render.",
          "items": {
            "$ref": "https://schema.thermidor.coveo.com/definitions/product.schema.json"
          }
        }
      },
      "additionalProperties": false
    }
  }
}
