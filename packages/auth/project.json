{
  "name": "auth",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "targets": {
    "release:phase1": {},
    "cached:build": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["pnpm run build:bundles", "pnpm run build:definitions"],
        "parallel": true,
        "cwd": "packages/auth"
      }
    },
    "build": {
      "dependsOn": ["cached:build"],
      "executor": "nx:noop"
    }
  }
}
