{
  "name": "@coveo/atomic-react-sample",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "namedInputs": {
    "negativeBuildOutputs": ["!{projectRoot}/public/dist"]
  },
  "targets": {
    "cached:build": {
      "outputs": ["{projectRoot}/public/dist"],
      "executor": "nx:run-commands",
      "options": {
        "commands": ["npm run build:vite"],
        "parallel": true,
        "cwd": "packages/samples/@coveo/atomic-react-sample"
      }
    },
    "build": {
      "dependsOn": ["cached:build"],
      "executor": "nx:noop"
    }
  }
}
