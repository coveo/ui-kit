{
  "name": "headless-ssr-samples",
  "targets": {
    "cached:build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm run build:next",
        "cwd": "packages/samples/headless-ssr"
      }
    },
    "build": {
      "dependsOn": ["cached:build"],
      "executor": "nx:noop"
    }
  }
}
