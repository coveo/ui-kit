{
  "name": "headless-ssr-commerce-samples-2",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "targets": {
    "cached:build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm run build:next",
        "cwd": "packages/samples/headless-ssr-commerce-2"
      }
    },
    "build": {
      "dependsOn": ["cached:build"],
      "executor": "nx:noop"
    }
  }
}
