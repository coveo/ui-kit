describe('Result Number Component', () => {
  describe('when not a child of an "template" element', () => {
    it.skip('should render an "atomic-component-error" component');
  });

  describe('when the field does not exist for the result', () => {
    it.skip('should remove the component from the DOM');
  });

  describe('when the field value cannot be parsed to a number', () => {
    // TODO: development required
    it.skip('should remove the component from the DOM');
  });

  describe('when the "currency" prop is not a valid currency', () => {
    it.skip('should remove the component from the DOM');
  });

  describe('when the field value is valid', () => {
    it.skip('should render the component');

    it.skip("the rendered value should respect the interface's language");

    it.skip('the rendered value should respect the "currency" prop');
  });
});
