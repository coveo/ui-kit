@import '../../../../global/global.pcss';

.hide-tabs {
  height: 0;
  visibility: hidden;
  margin: 0;
  padding: 0;
}

select:hover + div,
select:focus-visible + div {
  @apply text-primary-light;
}
