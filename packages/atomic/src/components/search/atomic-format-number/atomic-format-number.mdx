import { Meta } from '@storybook/addon-docs/blocks';
import * as AtomicFormatNumberStories from './atomic-format-number.new.stories';
import { AtomicDocTemplate } from '../../../../storybook-utils/documentation/atomic-doc-template';

<Meta of={AtomicFormatNumberStories} />


<AtomicDocTemplate
stories={AtomicFormatNumberStories}
  githubPath="search/atomic-format-number/atomic-format-number.ts"
  tagName="atomic-format-number"
  className="AtomicFormatNumber"
>

# atomic-format-number

This component configures number formatting for parent components that display numeric values. It doesn't render any content itself but dispatches formatting functions to its parent components.

The component is typically placed within components that display numeric values, such as facets or result fields.

## Usage in Numeric Facets

```html
<atomic-search-interface>
  ...
  <atomic-search-layout>
    ...
    <atomic-layout-section section="facets">
      
      <atomic-numeric-facet field="size">
        <atomic-format-number locale="en-US" maximum-fraction-digits="2"></atomic-format-number>
      </atomic-numeric-facet>

    </atomic-layout-section>
    ...
  </atomic-search-layout>
</atomic-search-interface>
```

## Usage in Result Fields

```html
<atomic-search-interface>
  ...
  <atomic-result-template>
    <template>
      <atomic-result-section-visual>
        <atomic-result-icon></atomic-result-icon>
      </atomic-result-section-visual>
      
      <atomic-result-section-title>
        <atomic-result-link></atomic-result-link>
      </atomic-result-section-title>

      <atomic-result-section-metadata>
        <atomic-result-number field="price">
          <atomic-format-number locale="en-US" maximum-fraction-digits="2"></atomic-format-number>
        </atomic-result-number>
      </atomic-result-section-metadata>
    </template>
  </atomic-result-template>
</atomic-search-interface>
```

The component accepts standard `Intl.NumberFormat` options as properties to control number formatting behavior.
</AtomicDocTemplate>
