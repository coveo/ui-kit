import { Meta } from '@storybook/addon-docs/blocks';
import meta, { Default } from './atomic-refine-modal.new.stories';
import { AtomicDocTemplate } from '@/storybook-utils/documentation/atomic-doc-template';


<Meta of={meta} />

<AtomicDocTemplate
  stories={{ Default }}
  githubPath="search/atomic-refine-modal/atomic-refine-modal.ts"
  tagName="atomic-refine-modal"
  className="AtomicRefineModal"
>

The `atomic-refine-modal` is an internal component. It is publicly documented for the sake of exposing its CSS properties and shadow parts so that you can customize its styles.

You should never insert this component directly in your markup or modify its attributes.

The correct way to use the refine modal is through the `atomic-refine-toggle` component, usually placed within the "status" section of the layout.
```html
<atomic-search-interface>
  ...
  <atomic-search-layout>
    ...
    <atomic-layout-section section="main">
      ...
      <atomic-layout-section section="status">

            <atomic-refine-toggle></atomic-refine-toggle>

      </atomic-layout-section>
    </atomic-layout-section>
  </atomic-search-layout>
</atomic-search-interface>
```

## Related Components

- `atomic-refine-toggle`: The toggle button that opens the refine modal
- `atomic-sort-dropdown`: Provides sort options for search results
- `atomic-facet`: Individual facet components that can be placed in the refine modal
- `atomic-breadbox`: Shows active filters and can be used alongside the refine modal

</AtomicDocTemplate>
