@import "../../global/global.pcss";

/**
 * @prop --max-breadcrumb-width: Maximum width of a breadcrumb before the text is truncated
 */
 .breadcrumb, .breadcrumb-label {
  max-width: var(--max-breadcrumb-width, 200px);
}

.breadcrumb-clear svg {
  @apply w-3 h-3;
}
