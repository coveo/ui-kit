atomic-icon-button::part(badge) {
  background: var(--atomic-primary);
  color: var(--atomic-on-primary);
  width: 1rem;
  display: block;
  height: 1rem;
  border-radius: 1rem;
  font-size: 0.6rem;
  line-height: 1rem;
  position: absolute;
  top: -0.4rem;
  right: -0.4rem;
  text-align: center;
}
