import { Meta } from '@storybook/addon-docs/blocks';
import * as AtomicInsightLayoutStories from './atomic-insight-layout.new.stories';
import { AtomicDocTemplate } from '@/storybook-utils/documentation/atomic-doc-template';

<Meta of={AtomicInsightLayoutStories} />

<AtomicDocTemplate
  stories={AtomicInsightLayoutStories}
  githubPath="insight/atomic-insight-layout/atomic-insight-layout.ts"
  tagName="atomic-insight-layout"
  className="AtomicInsightLayout"
>

This component organizes the insight page layout. 
Use `atomic-layout-section` to define the different sections of the page, such as **search**, **facets**, and **results**.

```html
<atomic-insight-layout>
  <atomic-layout-section section="search"></atomic-layout-section>
  <atomic-layout-section section="results"></atomic-layout-section>
</atomic-insight-layout>
```

</AtomicDocTemplate>
