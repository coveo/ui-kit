import { Meta } from '@storybook/addon-docs/blocks';
import meta, { Default, WithInputInteger, DisplayAsLink, Collapsed, WithSelectedValue } from './atomic-insight-numeric-facet.new.stories';
import { AtomicDocTemplate } from '../../../../storybook-utils/documentation/atomic-doc-template';

<Meta of={meta} />

<AtomicDocTemplate
  stories={{ Default, WithInputInteger, DisplayAsLink, Collapsed, WithSelectedValue }}
  githubPath="insight/atomic-insight-numeric-facet/atomic-insight-numeric-facet.ts"
  tagName="atomic-insight-numeric-facet"
  className="AtomicInsightNumericFacet"
>

This component displays numeric ranges as checkboxes or links for the Insight interface, commonly used for filtering by numeric fields such as view counts, ratings, or other quantifiable metrics.

````html
<atomic-insight-interface>
  ...
  <atomic-insight-layout>
    <atomic-layout-section section="facets">
      <atomic-facet-manager>

        <atomic-insight-numeric-facet
          field="ytviewcount"
          label="View Count"
          with-input="integer"
        ></atomic-insight-numeric-facet>

      </atomic-facet-manager>
    </atomic-layout-section>
  </atomic-insight-layout>
</atomic-insight-interface>
````

## UX Best Practices

- Checkboxes let users select multiple values and are the most commonly used selection, being more visual and presenting more values in small spaces.
- Links allow the user to select only one value at a time, behaving like a normal navigational link.
- Best used in cases where the value has units, such as height, width, screen sizes, view counts, etc.

## Reference
[Don't Lose Face with Facets](https://source.coveo.com/2018/04/18/facets/)
</AtomicDocTemplate>
