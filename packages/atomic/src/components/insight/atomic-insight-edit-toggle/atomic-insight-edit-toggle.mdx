import {Meta, Canvas, Story, Controls} from '@storybook/blocks';
import * as InsightEditToggleStories from './atomic-insight-edit-toggle.new.stories';

<Meta of={InsightEditToggleStories} />

# Atomic Insight Edit Toggle

The `atomic-insight-edit-toggle` component provides an edit button for insight interfaces. This is an internal component typically used within other Insight components to provide editing functionality.

<Canvas of={InsightEditToggleStories.Default} />

## Properties

<Controls />

## Usage

The component is primarily used internally but can be configured with a callback function and optional tooltip.

### With Tooltip

<Canvas of={InsightEditToggleStories.WithTooltip} />

### With Long Tooltip

<Canvas of={InsightEditToggleStories.WithLongTooltip} />

## Styling

The component uses the following part attributes for styling:

- `insight-edit-toggle-container`: The container wrapper
- `insight-edit-toggle-button`: The button element
- `insight-edit-toggle-icon`: The edit icon

## Accessibility

- The button has an `aria-label` of "Edit" for screen reader users
- Supports keyboard navigation and activation
- Tooltip text is displayed via the `title` attribute

## Notes

- This is an internal component marked with `@internal`
- The edit icon is built into the component
- The component uses light DOM for rendering
