@import '../../../global/global.pcss';

button {
  @apply relative pb-3 mt-1 mr-6 font-semibold;
}

.active {
  &::after {
    @apply block absolute rounded w-full bg-primary bottom-0 h-1;
    content: '';
  }
}
