@import '../../../global/global.pcss';

.active {
  &::after {
    @apply bg-primary absolute bottom-0 block h-1 w-full rounded;
    content: '';
  }
}

[part='tab'] {
  @apply relative mr-6 mt-1 pb-3 font-semibold;
  max-width: 150px;
  text-overflow: ellipsis;
  overflow: hidden;
}
