@import '../../../../global/global.pcss';

/* This stylesheet can be removed once atomic-insight-user-actions-modal is migrated to lit. 
The remaining styles can be moved to the static style attribute of the component once migrated */

atomic-modal::part(body-wrapper),
atomic-modal::part(footer-wrapper) {
  padding: 2px;
}
