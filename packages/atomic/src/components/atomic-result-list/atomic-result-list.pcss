/* CAREFUL! These styles aren't protected by a shadow DOM. */

atomic-result + atomic-result::before {
  content: ' ';
  display: block;
  height: 2px;
  margin: 0 2rem;
  background-color: var(--atomic-neutral);
}
