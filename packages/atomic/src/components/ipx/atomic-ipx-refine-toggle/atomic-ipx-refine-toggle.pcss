@import '../../../global/global.pcss';

atomic-icon-button {
  @apply pl-2 h-full;

  &::part(button) {
    @apply h-full w-auto;
  }

  &::part(badge) {
    @apply w-5 h-5 leading-5 text-xs;
  }

  &::part(icon) {
    @apply w-5 h-5;
  }
}
