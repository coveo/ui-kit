@import '../../../global/global.pcss';

atomic-icon-button {
  height: 100%;
  &::part(button) {
    aspect-ratio: 1 / 1;
    width: 3.6rem;
    height: 100%;
  }
  &::part(badge) {
    width: 1.25rem;
    height: 1.25rem;
    line-height: 1.25rem;
    font-size: 0.7rem;
  }
}
