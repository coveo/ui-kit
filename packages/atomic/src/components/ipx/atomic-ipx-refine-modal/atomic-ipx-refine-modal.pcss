@import '../../../global/global.pcss';

/* This stylesheet can be removed once atomic-ipx-refine-modal is migrated to lit.

The remaining styles can be moved to the static style attribute of the component once migrated, but the 'rounded' utility will need to be replaced by the equivalent css rules as we can't reference utilities in the static styles */

:host::part(container) {
  @apply rounded;
  max-height: calc(100vh - 4.25rem);
}
