@import '../../global/global.pcss';

button {
  @apply h-full w-10;
}

.btn-page {
  @apply h-full w-10;
  -webkit-appearance: none;
  -moz-appearance: none;
  appearance: none;

  &::before {
    display: inline;
    content: attr(value);
  }
}
