@import '../../global/global.pcss';

[part='buttons'] {
  @apply flex justify-end;

  @screen desktop-only {
    @apply gap-2;
  }

  @screen mobile-only {
    @apply gap-4;
  }
}

[part='cancel-button'],
[part='submit-button'] {
  @apply flex justify-center text-sm leading-4 p-2;
}
