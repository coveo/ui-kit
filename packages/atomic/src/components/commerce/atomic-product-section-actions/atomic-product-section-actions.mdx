import { Canvas, ArgTypes, Meta, Title, Description, Stories, Subtitle } from '@storybook/blocks';
import * as AtomicProductSectionActionsStories from './atomic-product-section-actions.new.stories';
import { CanvasWithGithub } from '../../../../storybook-utils/documentation/canvas-with-github';

<Meta of={AtomicProductSectionActionsStories} />

<Title />
<Subtitle><Description/></Subtitle>

<CanvasWithGithub
  of={AtomicProductSectionActionsStories.Default}
  githubPath="commerce/atomic-product-section-actions/atomic-product-section-actions.ts"
/>

## Usage
This section is intended to display components that allow the user to perform an action on a product without having to view its details.
In commerce interfaces, users can typically add products to their cart, wishlist, or perform other quick actions.

### Example within a product template
```html
<atomic-commerce-product-list>
  <atomic-product-template>
    <template>
      <!-- ... -->
      <atomic-product-section-actions>
        <button class="btn btn-primary">Add to Cart</button>
        <button class="btn btn-outline">Add to Wishlist</button>
      </atomic-product-section-actions>
      <!-- ... -->
    </template>
  </atomic-product-template>
</atomic-commerce-product-list>
```

### CSS Variables
The component exposes the following CSS variable for styling consistency:
- `--line-height`: The current line height that child elements should align to

### Styling Guidelines
- Child action buttons should use `height: var(--line-height)` for consistent sizing
- The section uses flexbox with gap spacing, so individual margins between actions are not needed
- Actions are displayed horizontally but will wrap if space is limited

## Examples
<Stories title={""}/>

## Reference
<ArgTypes />