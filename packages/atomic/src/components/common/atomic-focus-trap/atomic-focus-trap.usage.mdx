## Usage

The `atomic-focus-trap` component manages focus within a modal or dialog, ensuring keyboard navigation stays within the trap when active.

```html
<atomic-focus-trap active></atomic-focus-trap>
```
