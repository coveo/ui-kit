import { Meta } from '@storybook/addon-docs/blocks';
import meta, { Default } from './atomic-component-error.new.stories';
import { AtomicDocTemplate } from '@/storybook-utils/documentation/atomic-doc-template';

<Meta of={meta} />

<AtomicDocTemplate
  stories={{ Default }}
  githubPath="common/atomic-component-error/atomic-component-error.ts"
  tagName="atomic-component-error"
  className="AtomicComponentError"
>

The `atomic-component-error` component is used internally by other components to render and log errors when a component fails to initialize or encounters a runtime error.

```html
<atomic-component-error></atomic-component-error>
```

</AtomicDocTemplate>
