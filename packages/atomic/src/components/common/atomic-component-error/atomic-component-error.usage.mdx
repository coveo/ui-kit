## Usage

The `atomic-component-error` component is used internally by other components to render and log errors when a component fails to initialize or encounters an issue.

```html
<atomic-component-error></atomic-component-error>
```
