@import '../../../global/global.pcss';

@layer utilities {
  atomic-icon {
    @apply inline-block;
    fill: currentColor;
    @apply aspect-square-[auto];
    > svg {
      width: 100%;
      max-height: 100%;
      @apply aspect-square-[auto];
    }
  }
}
