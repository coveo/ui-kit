@import '../../../global/global.pcss';

atomic-icon {
  @apply inline-block;
  fill: currentColor;
  @apply aspect-square;
  > svg {
    width: 100%;
    max-height: 100%;
    @apply aspect-square;
  }
}
