import { Meta } from '@storybook/addon-docs/blocks';
import meta, { Default, AllIcons } from './atomic-icon.new.stories';
import { AtomicDocTemplate } from '@/storybook-utils/documentation/atomic-doc-template';


<Meta of={meta} />

<AtomicDocTemplate
  stories={{ Default, AllIcons }}
  githubPath="common/atomic-icon/atomic-icon.ts"
  tagName="atomic-icon"
  className="AtomicIcon"
>

## Usage
This component is used to display an icon in the UI. It is used internally for icon rendering and can be used directly in your Atomic layout.

```html
<atomic-icon icon="https://raw.githubusercontent.com/coveo/ui-kit/main/packages/atomic/src/images/arrow-top-rounded.svg"></atomic-icon>
```

</AtomicDocTemplate>
