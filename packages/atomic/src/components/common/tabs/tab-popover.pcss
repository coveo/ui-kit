@import '../../../global/global.pcss';

tabs-popover {
  position: absolute;
}

.visibility-hidden {
  visibility: hidden;
}

[part~='overflow-tabs'] {
  flex-direction: column;
  max-width: 150px;

  button {
    text-align: start;
  }
}
