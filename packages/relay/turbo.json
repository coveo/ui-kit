{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "outputs": ["lib/**"]
    },
    "functional-test": {
      "dependsOn": ["build"],
      "cache": false,
      "outputs": []
    }
  }
}
