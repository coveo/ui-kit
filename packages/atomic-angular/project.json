{
  "name": "atomic-angular-builder",
  "targets": {
    "release:phase1": {
      "executor": "nx:run-commands",
      "options": {
        "parallel": false,
        "command": "npx -p=@coveo/release bump",
        "cwd": "packages/atomic-angular/projects/atomic-angular"
      }
    },
    "build-new-version": {
      "dependsOn": ["release:phase1"],
      "executor": "nx:run-script",
      "options": {
        "script": "build"
      }
    },
    "release:phase2": {
      "dependsOn": ["release:phase1", "build-new-version"],
      "executor": "nx:run-commands",
      "options": {
        "command": "npx -p=@coveo/release npm-publish",
        "cwd": "packages/atomic-angular/projects/atomic-angular/dist"
      }
    }
  }
}
